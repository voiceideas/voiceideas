# Copy this file to .env.docker if you want authenticated CLIs inside the Codex container.
# The workspace .env continues to be mounted normally from the project root.

GH_TOKEN=
VERCEL_TOKEN=
SUPABASE_ACCESS_TOKEN=

# Optional git identity just for this isolated container.
GIT_AUTHOR_NAME=
GIT_AUTHOR_EMAIL=
